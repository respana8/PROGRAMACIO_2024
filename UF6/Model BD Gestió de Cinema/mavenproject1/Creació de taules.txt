CREATE TABLE Director (
    id INT PRIMARY KEY,
    nom VARCHAR(50),
    cognom VARCHAR(50)
);

CREATE TABLE Pelicula (
    id INT PRIMARY KEY,
    titol VARCHAR(50),
    anyEstrena DATE,
    directorId INT,
    FOREIGN KEY (directorId) REFERENCES Director(id)
);
